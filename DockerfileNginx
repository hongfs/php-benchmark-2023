FROM ghcr.io/hongfs/env:nginx-tp6-3

WORKDIR /hongfs

COPY . .

CMD envsubst '${WEB_DOMAIN},${WEB_PORT},${WEB_ROOT},${WEB_FASTCGI}' < /etc/nginx/templates/default.conf > /etc/nginx/conf.d/default.conf && nginx -g 'daemon off;'

